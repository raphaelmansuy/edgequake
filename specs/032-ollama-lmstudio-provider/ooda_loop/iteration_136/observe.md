# Iteration 136 – Observe

## Focus: Embedding Model Change at Workspace Level (Item 20)

### Requirement
> Ensure the embedding model can be changed at the workspace level, and when changed the vector database is recreated with the new embedding model.

### Current State

Need to verify:
1. Workspace settings allow embedding model change
2. API supports embedding model update
3. Rebuild mechanism exists for re-embedding
