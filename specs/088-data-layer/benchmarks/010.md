# Benchmark 010 — `DATA-PG-VECTORS-GET-BY-IDS-010`

**Entry:** `VectorStorage::get_by_ids` @ `edgequake/crates/edgequake-storage/src/adapters/postgres/vector/storage_impl.rs:354`

## Complexity

- Variables: N=table rows, K=result/limit, B=batch
- Time: O(log N) indexed; O(N) scan
- Space: O(K)
- I/O: index or seq

## EXPLAIN evidence

_Pending live capture._ Complexity class recorded in complexity-matrix.md.

Failure mode: timeout / lock wait / OOM on unbounded SELECT

## Captured EXPLAIN

```
/* DATA-PG-VECTORS-GET-BY-IDS-010 */

```
