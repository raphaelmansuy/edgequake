# Benchmark 185 — `DATA-PG-ENTITY-REMOVE-SOURCES-185`

**Entry:** `remove_entity_sources` @ `edgequake/crates/edgequake-api/src/postgres_entity_sink.rs:132`

## Complexity

- Variables: N=table rows, K=result/limit, B=batch
- Time: O(log N) indexed; O(N) scan
- Space: O(K)
- I/O: index or seq

## EXPLAIN evidence

_Pending live capture._ Complexity class recorded in complexity-matrix.md.

Failure mode: timeout / lock wait / OOM on unbounded SELECT

## Captured EXPLAIN

```
/* DATA-PG-ENTITY-REMOVE-SOURCES-185 */
Index Only Scan using eq_d088_rel_p4446_s91_t186_pkey on eq_d088_rel_p4446_s91_t186  (cost=0.15..8.17 rows=1 width=32)
  Index Cond: (id = 'id-1'::text)
```
