# Benchmark 017 — `DATA-PGVEC-VECTORS-WARMUP-ANN-017`

**Entry:** `VectorStorage::warmup_workspace_ann` @ `edgequake/crates/edgequake-storage/src/adapters/postgres/vector/storage_impl.rs:711`

## Complexity

- Variables: N=rows, K=top_k, D=dim, ef=ef_search
- Time: O(ef * log N) expected ANN; worst O(N) if seq scan
- Space: O(K + ef)
- I/O: ~O(ef) random pages; index residency critical

## EXPLAIN evidence

_Pending live capture._ Complexity class recorded in complexity-matrix.md.

Failure mode: timeout / under-K results / silent recall loss / seq-scan cliff

## Captured EXPLAIN

```
/* DATA-PGVEC-VECTORS-WARMUP-ANN-017 */
Limit  (cost=1.09..1.10 rows=2 width=40)
  ->  Sort  (cost=1.09..1.10 rows=4 width=40)
        Sort Key: ((embedding <=> '[1,0,0]'::vector))
        ->  Seq Scan on eq_d088_vec_360_18c588fc  (cost=0.00..1.05 rows=4 width=40)
```
