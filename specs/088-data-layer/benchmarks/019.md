# Benchmark 019 — `DATA-PGVEC-VECTORS-DDL-ENSURE-ANN-INDEX-019`

**Entry:** `ensure_ann_index` @ `edgequake/crates/edgequake-storage/src/adapters/postgres/vector/ddl.rs:234`

## Complexity

- Variables: N=rows, K=top_k, D=dim, ef=ef_search
- Time: O(ef * log N) expected ANN; worst O(N) if seq scan
- Space: O(K + ef)
- I/O: ~O(ef) random pages; index residency critical

## EXPLAIN evidence

_Pending live capture._ Complexity class recorded in complexity-matrix.md.

Failure mode: timeout / under-K results / silent recall loss / seq-scan cliff
