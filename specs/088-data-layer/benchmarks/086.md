# Benchmark 086 — `DATA-PG-KV-KEYS-WITH-PREFIX-LIMITED-086`

**Entry:** `KVStorage::keys_with_prefix_limited` @ `edgequake/crates/edgequake-storage/src/adapters/postgres/kv.rs:487`

## Complexity

- Variables: N=keys, K=batch, M=prefix matches
- Time: O(log N) single; O(K log N) batch; O(M) prefix
- Space: O(K) or O(M)
- I/O: index + heap per key

## EXPLAIN evidence

_Pending live capture._ Complexity class recorded in complexity-matrix.md.

Failure mode: error on multi-workspace key batch; O(N) COUNT* fallback

## Captured EXPLAIN

```
/* DATA-PG-KV-KEYS-WITH-PREFIX-LIMITED-086 */
Index Scan using eq_d088_kv_360_18c588fc_pkey on eq_d088_kv_360_18c588fc  (cost=0.15..8.17 rows=1 width=32)
  Index Cond: (key = 'a'::text)
```
