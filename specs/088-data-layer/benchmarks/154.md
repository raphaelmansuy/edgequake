# Benchmark 154 — `DATA-PG-WORKSPACE-UPDATE-154`

**Entry:** `pg_update_workspace` @ `edgequake/crates/edgequake-core/src/workspace_service_impl/workspace_ops.rs:286`

## Complexity

- Variables: N=table rows, K=result/limit, B=batch
- Time: O(log N) indexed; O(N) scan
- Space: O(K)
- I/O: index or seq

## EXPLAIN evidence

_Pending live capture._ Complexity class recorded in complexity-matrix.md.

Failure mode: timeout / lock wait / OOM on unbounded SELECT

## Captured EXPLAIN

```
/* DATA-PG-WORKSPACE-UPDATE-154 */
Index Only Scan using eq_d088_rel_p4446_s189_t424_pkey on eq_d088_rel_p4446_s189_t424  (cost=0.15..8.17 rows=1 width=32)
  Index Cond: (id = 'id-1'::text)
```
