# Benchmark 202 — `DATA-PG-CONFIG-LOAD-PRIORITY-MODE-202`

**Entry:** `load_priority_mode` @ `edgequake/crates/edgequake-api/src/server_config_store.rs:202`

## Complexity

- Variables: N=table rows, K=result/limit, B=batch
- Time: O(log N) indexed; O(N) scan
- Space: O(K)
- I/O: index or seq

## EXPLAIN evidence

_Pending live capture._ Complexity class recorded in complexity-matrix.md.

Failure mode: timeout / lock wait / OOM on unbounded SELECT

## Captured EXPLAIN

```
/* DATA-PG-CONFIG-LOAD-PRIORITY-MODE-202 */
Index Only Scan using eq_d088_rel_p4446_s61_t126_pkey on eq_d088_rel_p4446_s61_t126  (cost=0.15..8.17 rows=1 width=32)
  Index Cond: (id = 'id-1'::text)
```
