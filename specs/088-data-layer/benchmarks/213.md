# Benchmark 213 — `DATA-PG-INSPECT-APPLY-REPAIR-213`

**Entry:** `apply_repair` @ `edgequake/crates/edgequake-api/src/storage_inspector.rs:1187`

## Complexity

- Variables: N=table rows, K=result/limit, B=batch
- Time: O(log N) indexed; O(N) scan
- Space: O(K)
- I/O: index or seq

## EXPLAIN evidence

_Pending live capture._ Complexity class recorded in complexity-matrix.md.

Failure mode: timeout / lock wait / OOM on unbounded SELECT

## Captured EXPLAIN

```
/* DATA-PG-INSPECT-APPLY-REPAIR-213 */
Seq Scan on pg_extension  (cost=0.00..1.01 rows=1 width=64)
  Filter: (extname = 'vector'::name)
```
