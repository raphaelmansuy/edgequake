# Benchmark 140 — `DATA-PG-TASKS-CLAIM-NEXT-140`

**Entry:** `PostgresTaskStorage::claim_next` @ `edgequake/crates/edgequake-tasks/src/postgres.rs:500`

## Complexity

- Variables: N=tasks, W=active workspaces
- Time: O(log N) get; claim O(W + log N) + row lock
- Space: O(1) claim; O(page) list
- I/O: index on status/workspace/lease

## EXPLAIN evidence

Representative plan shape (capture with live DB):

```sql
/* DATA-PG-TASKS-CLAIM-NEXT-140 */ EXPLAIN (ANALYZE, BUFFERS, VERBOSE, SETTINGS, FORMAT TEXT)
-- bind representative parameters for this operation
```

### Expected access path

- Engine: PG
- Prefer index path documented in indexes.md
- Fail test if plan regresses to Seq Scan on N > threshold (see Phase 3)

## Scaling (relative)

| N | Relative latency class |
|---|---|
| 1e3 | baseline |
| 1e5 | ≤ 5× baseline (log-ish) for indexed ops |
| 1e6 | ≤ 10× baseline for ANN/PK; fail if linear full scan |

Absolute wall-clock lives in non-blocking nightly benchmark job.

## Captured EXPLAIN

```
/* DATA-PG-TASKS-CLAIM-NEXT-140 */
Limit  (cost=9.51..9.52 rows=1 width=40)
  ->  Sort  (cost=9.51..9.52 rows=2 width=40)
        Sort Key: created_at
        ->  Bitmap Heap Scan on eq_d088_tasks_360_18c588fc  (cost=4.16..9.50 rows=2 width=40)
              Recheck Cond: (status = 'pending'::text)
              ->  Bitmap Index Scan on eq_d088_tasks_360_18c588fc_status_workspace_id_created_at_idx  (cost=0.00..4.16 rows=2 width=0)
                    Index Cond: (status = 'pending'::text)
```
