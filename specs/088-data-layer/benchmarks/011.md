# Benchmark 011 — `DATA-PG-VECTORS-COUNT-011`

**Entry:** `VectorStorage::count` @ `edgequake/crates/edgequake-storage/src/adapters/postgres/vector/storage_impl.rs:394`

## Complexity

- Variables: N=table rows, K=result/limit, B=batch
- Time: O(log N) indexed; O(N) scan
- Space: O(K)
- I/O: index or seq

## EXPLAIN evidence

_Pending live capture._ Complexity class recorded in complexity-matrix.md.

Failure mode: timeout / lock wait / OOM on unbounded SELECT

## Captured EXPLAIN

```
/* DATA-PG-VECTORS-COUNT-011 */
Limit  (cost=1.09..1.10 rows=2 width=40)
  ->  Sort  (cost=1.09..1.10 rows=4 width=40)
        Sort Key: ((embedding <=> '[1,0,0]'::vector))
        ->  Seq Scan on eq_d088_vec_p4446_s174_t391  (cost=0.00..1.05 rows=4 width=40)
```
