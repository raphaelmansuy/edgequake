# Benchmark 138 — `DATA-PG-TASKS-FIND-ACTIVE-PDF-138`

**Entry:** `PostgresTaskStorage::find_active_pdf_processing_task` @ `edgequake/crates/edgequake-tasks/src/postgres.rs:429`

## Complexity

- Variables: N=tasks, W=active workspaces
- Time: O(log N) get; claim O(W + log N) + row lock
- Space: O(1) claim; O(page) list
- I/O: index on status/workspace/lease

## EXPLAIN evidence

_Pending live capture._ Complexity class recorded in complexity-matrix.md.

Failure mode: starvation without fair claim; lease expiry race

## Captured EXPLAIN

```
/* DATA-PG-TASKS-FIND-ACTIVE-PDF-138 */
Limit  (cost=9.51..9.52 rows=1 width=40)
  ->  Sort  (cost=9.51..9.52 rows=2 width=40)
        Sort Key: created_at
        ->  Bitmap Heap Scan on eq_d088_tasks_p4446_s156_t355  (cost=4.16..9.50 rows=2 width=40)
              Recheck Cond: (status = 'pending'::text)
              ->  Bitmap Index Scan on eq_d088_tasks_p4446_s156_t355_status_workspace_id_created_a_idx  (cost=0.00..4.16 rows=2 width=0)
                    Index Cond: (status = 'pending'::text)
```
