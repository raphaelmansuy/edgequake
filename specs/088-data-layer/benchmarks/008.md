# Benchmark 008 — `DATA-PG-VECTORS-DELETE-ENTITY-RELATIONS-008`

**Entry:** `VectorStorage::delete_entity_relations` @ `edgequake/crates/edgequake-storage/src/adapters/postgres/vector/storage_impl.rs:314`

## Complexity

- Variables: N=table rows, K=result/limit, B=batch
- Time: O(log N) indexed; O(N) scan
- Space: O(K)
- I/O: index or seq

## EXPLAIN evidence

_Pending live capture._ Complexity class recorded in complexity-matrix.md.

Failure mode: timeout / lock wait / OOM on unbounded SELECT

## Captured EXPLAIN

```
/* DATA-PG-VECTORS-DELETE-ENTITY-RELATIONS-008 */

```
