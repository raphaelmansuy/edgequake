# Benchmark 076 — `DATA-PG-KV-GET-BY-IDS-076`

**Entry:** `KVStorage::get_by_ids` @ `edgequake/crates/edgequake-storage/src/adapters/postgres/kv.rs:214`

## Complexity

- Variables: N=keys, K=batch, M=prefix matches
- Time: O(log N) single; O(K log N) batch; O(M) prefix
- Space: O(K) or O(M)
- I/O: index + heap per key

## EXPLAIN evidence

Representative plan shape (capture with live DB):

```sql
/* DATA-PG-KV-GET-BY-IDS-076 */ EXPLAIN (ANALYZE, BUFFERS, VERBOSE, SETTINGS, FORMAT TEXT)
-- bind representative parameters for this operation
```

### Expected access path

- Engine: PG
- Prefer index path documented in indexes.md
- Fail test if plan regresses to Seq Scan on N > threshold (see Phase 3)

## Scaling (relative)

| N | Relative latency class |
|---|---|
| 1e3 | baseline |
| 1e5 | ≤ 5× baseline (log-ish) for indexed ops |
| 1e6 | ≤ 10× baseline for ANN/PK; fail if linear full scan |

Absolute wall-clock lives in non-blocking nightly benchmark job.

## Captured EXPLAIN

```
/* DATA-PG-KV-GET-BY-IDS-076 */
Index Scan using eq_d088_kv_p4446_s106_t226_pkey on eq_d088_kv_p4446_s106_t226  (cost=0.15..8.17 rows=1 width=32)
  Index Cond: (key = 'a'::text)
```
