# Benchmark 171 — `DATA-PG-AUTH-SYNC-MEMBERSHIP-171`

**Entry:** `sync_default_membership_to_postgres` @ `edgequake/crates/edgequake-api/src/services/identity_storage.rs:239`

## Complexity

- Variables: N=table rows, K=result/limit, B=batch
- Time: O(log N) indexed; O(N) scan
- Space: O(K)
- I/O: index or seq

## EXPLAIN evidence

_Pending live capture._ Complexity class recorded in complexity-matrix.md.

Failure mode: timeout / lock wait / OOM on unbounded SELECT

## Captured EXPLAIN

```
/* DATA-PG-AUTH-SYNC-MEMBERSHIP-171 */
Index Only Scan using eq_d088_rel_p4446_s57_t118_pkey on eq_d088_rel_p4446_s57_t118  (cost=0.15..8.17 rows=1 width=32)
  Index Cond: (id = 'id-1'::text)
```
