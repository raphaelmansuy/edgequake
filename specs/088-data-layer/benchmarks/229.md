# Benchmark 229 — `DATA-PG-SCHEMA-MIG-HNSW-OPTIMIZE-229`

**Entry:** `migration 071` @ `edgequake/migrations/071_hnsw_optimize.sql:1`

## Complexity

- Variables: N=rows for index build
- Time: O(N log N) HNSW build; O(N) btree
- Space: maintenance_work_mem
- I/O: full table scan build

## EXPLAIN evidence

_Pending live capture._ Complexity class recorded in complexity-matrix.md.

Failure mode: lock contention ACCESS EXCLUSIVE; build OOM
