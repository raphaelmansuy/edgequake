# Decision - Iteration 10

## Changes
1. `types/index.ts` — Add 4 optional fields to `Document`
2. `types/lineage.ts` — Add `start_line`/`end_line` to `ChunkDetail`, add 2 new response types
3. `components/document/source-info-grid.tsx` — Display document_type, page_count, sha256
