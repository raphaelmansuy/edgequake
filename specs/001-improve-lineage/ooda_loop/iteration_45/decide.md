# Decision - Iteration 45

## Changes to Make

None — pattern is already consistent across all right panels.

## Expected Outcome

All scrollable panels follow the same canonical pattern with `overflow-hidden`, `shrink-0`, `min-h-0`, and `showShadows`.
