# Decision - Iteration 15
1. Add `DocumentFullLineageResponse` and `ChunkLineageResponse` types to `types/lineage.ts`
2. Add `getLineage()`, `getMetadata()` to `DocumentsResource`
3. Add `getLineage()` to `ChunksResource`
