## Roadmap


- Add support to other format using markdownify crate (PDF, DOCX, HTML etc.)

Other format: https://crates.io/crates/markdownify

- Add true multinenant support with namespace isolation at the API and storage level

- Add a nice docusaurus based documentation site for EdgeQuake


- Add a better query chat interface with message history, better prompt management, streaming etc.

- Improve and battle test the Layout of every pages (Graph, Documents, Query etc.)

- Refine de design and user documentation

- Add the support of multi LLM providers

- Add the support of evaluation suite for RAG systems (RAGAS, mlflow etc.)

- Ensure metadata management for documents and graph entities (page numbers, source documents etc.)
