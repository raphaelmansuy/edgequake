# Orient – OODA-21: Add FormulaConfig Builder Tests

## Strategy

Add 3 simple unit tests for the FormulaConfig builder methods.

## Tests to Add

1. **`test_formula_config_with_min_density`**: Verify builder sets value correctly
2. **`test_formula_config_with_min_confidence`**: Verify builder sets value correctly
3. **`test_formula_config_new_equals_default`**: Verify `new()` and `Default` are equivalent

## Implementation

These are simple builder tests that verify the builder pattern works correctly.
