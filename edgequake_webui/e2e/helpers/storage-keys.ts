/** Zustand persist key for tenant/workspace selection (must match WebUI store). */
export const ZUSTAND_TENANT_KEY = "edgequake-tenant";

/** Legacy keys — do not use in new tests. */
export const LEGACY_TENANT_STORE_KEY = "edgequake-tenant-store";
