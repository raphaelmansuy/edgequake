[package]
name = "edgequake-api"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "REST API server for EdgeQuake"

[features]
default = []
otel = ["edgequake-observability/otel"]
postgres = ["edgequake-storage/postgres", "edgequake-core/postgres", "dep:sqlx"]
# SPEC-021 P-E3: enables the EdgeQuake orchestrator for contract tests.
# Forwarded to edgequake-core so `#[cfg(feature = "pipeline")]` test gates work.
pipeline = ["edgequake-core/pipeline"]
# Vision mode for PDF extraction using edgequake-pdf2md (bundled pdfium, no system deps)
vision = []

[dependencies]
edgequake-core = { path = "../edgequake-core" }
edgequake-pdf = { workspace = true }
edgequake-storage = { path = "../edgequake-storage" }
edgequake-llm = { workspace = true }
edgequake-pipeline = { path = "../edgequake-pipeline" }
edgequake-query = { path = "../edgequake-query" }
edgequake-tasks = { path = "../edgequake-tasks", features = ["postgres", "openapi"] }
edgequake-auth = { path = "../edgequake-auth" }
edgequake-rate-limiter = { path = "../edgequake-rate-limiter" }
edgequake-audit = { path = "../edgequake-audit" }
edgequake-observability = { path = "../edgequake-observability", features = ["metrics"] }
# Direct dependency kept because the API crate implements the progress callback type.
edgequake-pdf2md = { workspace = true }

axum.workspace = true
axum-extra = { version = "0.10", features = ["multipart"] }
tokio = { workspace = true, features = ["full"] }
tokio-stream = "0.1"
futures-util = "0.3"
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
thiserror.workspace = true
tracing.workspace = true
tracing-subscriber = { version = "0.3", features = ["json", "env-filter"] }
tower = { version = "0.5", features = ["timeout", "limit"] }
tower-http = { version = "0.6", features = [
    "cors",
    "trace",
    "compression-gzip",
] }
utoipa.workspace = true
utoipa-swagger-ui = { version = "9", features = ["axum"] }
uuid.workspace = true
futures.workspace = true
chrono = { version = "0.4", features = ["serde"] }
base64 = "0.22"
md5 = { workspace = true }
regex = "1"
async-trait.workspace = true
sqlx = { workspace = true, optional = true }
reqwest = { version = "0.12", features = ["blocking", "json"] }
openidconnect = { version = "4.0", default-features = false, features = ["reqwest"] }
url = "2.5"
lazy_static = "1.5"

# File handling
sha2 = "0.10"
hex = "0.4"
rand = "0.9"
bytes = "1.9"
tokio-util = "0.7"
# SSE streaming for real-time PDF progress events
async-stream = "0.3"

[dev-dependencies]
edgequake-tasks = { path = "../edgequake-tasks", features = ["postgres"] }
# SPEC-021 P-E3: enable the orchestrator (pipeline) for the ingest→delete
# contract test. The `pipeline` feature is also added to the api crate so
# `#[cfg(feature = "pipeline")]` gates in tests activate during `cargo test`.
edgequake-core = { path = "../edgequake-core", features = ["pipeline"] }
tokio = { workspace = true, features = ["full", "macros", "rt-multi-thread"] }
tower = { version = "0.5", features = ["util"] }
sqlx = { workspace = true, features = [
    "runtime-tokio",
    "postgres",
    "uuid",
    "chrono",
] }
serial_test = "3.2"
tempfile = "3"
urlencoding = "2.1"
wiremock = "0.6"
jsonwebtoken = "9.3"
