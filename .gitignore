# Rust build artifacts
target/
# WHY: Cargo.lock is committed for the application binary (edgequake/) to ensure
# reproducible CI builds. SDK libraries ignore their Cargo.lock (standard practice).
sdks/*/Cargo.lock

# Swift SDK build artifacts (SPM) — must stay untracked even if built locally
sdks/swift/.build/
sdks/swift/.swiftpm/
sdks/**/.build/
sdks/**/.swiftpm/

# Java SDK — Maven target/ is covered by root target/; Eclipse/m2e often
# materializes a mirrored `bin/` tree with .class + copied sources (do not commit).
sdks/java/bin/
sdks/**/bin/
*.class

**/*.rs.bk
*.pdb

# IDE and editor files
 .idea/
*.swp
*.swo
*~
.DS_Store

# Screenshots / temporary images at repo root
*.png
*.jpg
*.jpeg
!specs/017-dry-and-solid-audit/005-edgequake-pipeline/e2e/screenshots/*.png
!specs/017-dry-and-solid-audit/006-edgequake-query/e2e/screenshots/*.png
!specs/017-dry-and-solid-audit/007-edgequake-storage/e2e/screenshots/*.png
!specs/096-multi-language-extraction/e2e/screenshots/*.png
!specs/128-improve-pdf-parsing/e2e/screenshots/*.png

# Environment files
.env
.edgequake-dev-ports.env
edgequake_webui/.env.local.ports
target/
edgequake/target/
edgequake/crates/*/target/
.env.*.local
*.env

# Sauvegardes et variantes locales de .env (peuvent contenir des secrets).
# Les motifs `.env` / `*.env` ne couvrent PAS `.env.backup-*` ni `.env.<suffixe>`.
.env.*
!.env.example
!.env.*.example

# Log files
*.log
lightrag.log*
logs/
/.playwright-mcp
archive/
logf/
zz_test_docs/
zz-explore/

# Python artifacts (for LightRAG reference)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
# Don't ignore Next.js lib directory
!**/src/lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.venv/
venv/
ENV/
edgequake_webui/node_modules/
edgequake_webui/.next/
edgequake_webui/.turbo/
edgequake_webui/playwright-report/
edgequake_webui/test-results/
edgequake_webui/build_pid.txt
edgequake_webui/tsc_output.txt
edgequake_webui/tsc_pid.txt
node_modules/
# Node.js (for webui)
node_modules/
.bun/
bun.lockb

# Playwright MCP artifacts
.playwright-mcp/

# Database and storage
*.db
*.sqlite
*.sqlite3
rag_storage/
temp/
inputs/
*.index
postgres_data/
pgdata/

# Test artifacts
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/

# Documentation build
site/
_build/

# Temporary files
*.tmp
*.temp
*.bak
*.orig

# Kubernetes secrets
k8s-deploy/secrets/

# Configuration with secrets
config.ini
*.pem
*.key

open-webui/

# Exploration and scratch work
zz-explore/

# Marketing website (separate project, not tracked here)
edgequake-website/

# Root-level marketing screenshots (safe to regenerate)
/*.png

# Terraform local state, plans, and var files (generated passwords / provider keys).
**/*.tfstate
**/*.tfstate.*
**/*.tfplan
**/terraform.tfvars
**/*.auto.tfvars
**/.terraform/

# SPEC-147 — local source PDFs (customer documents; do not commit to the public repo).
specs/147-fix-doc/data/*.pdf

# SPEC-134 Slice E — private page-as-unit study outputs / local gold (never commit trigger scans).
specs/134-manuscrit/study/out/
specs/134-manuscrit/study/gold.local.json
specs/134-manuscrit/study/.env

# SPEC-097 / GH-351 — fat regenerable bench001 archives (local-only).
# Keep thin scorecard/SUMMARY/BUSINESS_REPORT/meta in git; regenerate fat via make bench001-*.
specs/001-benchmark/e2e/artifacts/**/predictions_*.json
specs/001-benchmark/e2e/artifacts/**/eval_*.json
specs/001-benchmark/e2e/artifacts/**/eval_*.raw.json
specs/001-benchmark/e2e/artifacts/**/logs/progress.jsonl
specs/001-benchmark/e2e/artifacts/history/**/logs/

# OS files
Thumbs.db
ehthumbs.db
./metals
.edgecrab-artifacts/
specs/93-migration-assessment/reports/*/pre-upgrade.dump
.freebuff/

# Local cargo target override (SPEC-150 builds)
.cargo-target/
