# Complexity matrix

| Ref ID | Engine | Domain | Time | Space | Key limits | Failure |
|---|---|---|---|---|---|---|
| `DATA-PGVEC-VECTORS-ANN-QUERY-001` | PGVEC | VECTORS | O(ef * log N) expected ANN; worst O(N) if seq scan | O(K + ef) | ef_search = clamp(4*K, 40, 1000); iterative_scan required for filtered (max_scan_tuples=20000) | timeout / under-K results / silent recall loss / seq-scan cliff |
| `DATA-PGVEC-VECTORS-ANN-QUERY-FILTERED-002` | PGVEC | VECTORS | O(ef * log N) expected ANN; worst O(N) if seq scan | O(K + ef) | ef_search = clamp(4*K, 40, 1000); iterative_scan required for filtered (max_scan_tuples=20000) | timeout / under-K results / silent recall loss / seq-scan cliff |
| `DATA-PG-VECTORS-TEXT-SEARCH-FILTERED-003` | PG | VECTORS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PGVEC-VECTORS-UPSERT-BATCH-004` | PGVEC | VECTORS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-VECTORS-DELETE-BY-ID-005` | PG | VECTORS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-VECTORS-DELETE-ENTITY-006` | PG | VECTORS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-VECTORS-DELETE-ENTITIES-BATCH-007` | PG | VECTORS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-VECTORS-DELETE-ENTITY-RELATIONS-008` | PG | VECTORS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-VECTORS-GET-BY-ID-009` | PG | VECTORS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-VECTORS-GET-BY-IDS-010` | PG | VECTORS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-VECTORS-COUNT-011` | PG | VECTORS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-VECTORS-IS-EMPTY-012` | PG | VECTORS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-VECTORS-PING-013` | PG | VECTORS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-VECTORS-CLEAR-014` | PG | VECTORS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-VECTORS-CLEAR-WORKSPACE-015` | PG | VECTORS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-VECTORS-DELETE-BY-DOCUMENT-016` | PG | VECTORS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PGVEC-VECTORS-WARMUP-ANN-017` | PGVEC | VECTORS | O(ef * log N) expected ANN; worst O(N) if seq scan | O(K + ef) | ef_search = clamp(4*K, 40, 1000); iterative_scan required for filtered (max_scan_tuples=20000) | timeout / under-K results / silent recall loss / seq-scan cliff |
| `DATA-PGVEC-VECTORS-DDL-CREATE-TABLE-018` | PGVEC | VECTORS | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PGVEC-VECTORS-DDL-ENSURE-ANN-INDEX-019` | PGVEC | VECTORS | O(ef * log N) expected ANN; worst O(N) if seq scan | O(K + ef) | ef_search = clamp(4*K, 40, 1000); iterative_scan required for filtered (max_scan_tuples=20000) | timeout / under-K results / silent recall loss / seq-scan cliff |
| `DATA-PGVEC-VECTORS-DDL-PARTIAL-HNSW-020` | PGVEC | VECTORS | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-VECTORS-DDL-ENSURE-FTS-021` | PG | VECTORS | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PGVEC-VECTORS-SESSION-SEARCH-TUNING-022` | PGVEC | VECTORS | O(1) | O(1) | SET LOCAL only inside short transactions; Do not leak GUCs on pooled conns | GUC leak → wrong recall/plan for next borrower |
| `DATA-PG-VECTORS-WS-DROP-TABLE-023` | PG | VECTORS | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PGVEC-VECTORS-DIM-RECONCILE-024` | PGVEC | VECTORS | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-AGE-GRAPH-HAS-NODE-025` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-GET-NODE-026` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-NODE-DEGREE-027` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-NODE-DEGREES-BATCH-028` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-GET-ALL-NODES-029` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-GET-NODES-BY-IDS-030` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-GET-NODES-BATCH-031` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-GET-EDGES-FOR-NODES-BATCH-032` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-HAS-EDGE-033` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-GET-EDGE-034` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-GET-NODE-EDGES-035` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-GET-INCIDENT-EDGES-BATCH-036` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-GET-ALL-EDGES-037` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-GET-KNOWLEDGE-GRAPH-038` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-GET-POPULAR-LABELS-039` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-SEARCH-LABELS-040` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-SEARCH-NODES-041` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-GET-NEIGHBORS-042` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-GET-POPULAR-NODES-DEGREE-043` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-GET-EDGES-FOR-NODE-SET-044` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-UPSERT-NODE-045` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-UPSERT-NODES-BATCH-046` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-DELETE-NODE-047` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-DELETE-NODES-BATCH-048` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-DELETE-NODE-SCOPED-049` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-UPSERT-EDGE-050` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-UPSERT-EDGES-BATCH-051` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-DELETE-EDGE-052` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-DELETE-EDGES-BATCH-053` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-DELETE-EDGE-SCOPED-054` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-CLEAR-055` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-CLEAR-WORKSPACE-056` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-NODE-COUNT-057` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-EDGE-COUNT-058` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-NODE-COUNT-FAST-059` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-EDGE-COUNT-FAST-060` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-NODE-COUNT-BY-WORKSPACE-061` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-EDGE-COUNT-BY-WORKSPACE-062` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-DISTINCT-NODE-TYPE-COUNT-063` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-NODE-COUNT-BY-SOURCE-PREFIX-064` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-NODE-COUNTS-BY-SOURCE-PREFIXES-065` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-LIST-NODES-FILTERED-066` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-LIST-EDGES-FILTERED-067` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-FIND-NODES-BY-SOURCE-PREFIXES-068` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-FIND-EDGES-BY-SOURCE-PREFIXES-069` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-FIND-EDGE-BY-RELATIONSHIP-ID-070` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-CYPHER-EXEC-071` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-LIFECYCLE-ENSURE-INDEXES-072` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-COPY-LOAD-VERTICES-073` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-AGE-GRAPH-SESSION-LOAD-AGE-074` | AGE | GRAPH | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-PG-KV-GET-BY-ID-075` | PG | KV | O(log N) single; O(K log N) batch; O(M) prefix | O(K) or O(M) | Batch chunk ≤1000 on upsert; Postgres bind param cap 65535 | error on multi-workspace key batch; O(N) COUNT* fallback |
| `DATA-PG-KV-GET-BY-IDS-076` | PG | KV | O(log N) single; O(K log N) batch; O(M) prefix | O(K) or O(M) | Batch chunk ≤1000 on upsert; Postgres bind param cap 65535 | error on multi-workspace key batch; O(N) COUNT* fallback |
| `DATA-PG-KV-GET-BY-IDS-ORDERED-077` | PG | KV | O(log N) single; O(K log N) batch; O(M) prefix | O(K) or O(M) | Batch chunk ≤1000 on upsert; Postgres bind param cap 65535 | error on multi-workspace key batch; O(N) COUNT* fallback |
| `DATA-PG-KV-FILTER-KEYS-078` | PG | KV | O(log N) single; O(K log N) batch; O(M) prefix | O(K) or O(M) | Batch chunk ≤1000 on upsert; Postgres bind param cap 65535 | error on multi-workspace key batch; O(N) COUNT* fallback |
| `DATA-PG-KV-UPSERT-079` | PG | KV | O(log N) single; O(K log N) batch; O(M) prefix | O(K) or O(M) | Batch chunk ≤1000 on upsert; Postgres bind param cap 65535 | error on multi-workspace key batch; O(N) COUNT* fallback |
| `DATA-PG-KV-DELETE-080` | PG | KV | O(log N) single; O(K log N) batch; O(M) prefix | O(K) or O(M) | Batch chunk ≤1000 on upsert; Postgres bind param cap 65535 | error on multi-workspace key batch; O(N) COUNT* fallback |
| `DATA-PG-KV-COUNT-081` | PG | KV | O(log N) single; O(K log N) batch; O(M) prefix | O(K) or O(M) | Batch chunk ≤1000 on upsert; Postgres bind param cap 65535 | error on multi-workspace key batch; O(N) COUNT* fallback |
| `DATA-PG-KV-IS-EMPTY-082` | PG | KV | O(log N) single; O(K log N) batch; O(M) prefix | O(K) or O(M) | Batch chunk ≤1000 on upsert; Postgres bind param cap 65535 | error on multi-workspace key batch; O(N) COUNT* fallback |
| `DATA-PG-KV-PING-083` | PG | KV | O(log N) single; O(K log N) batch; O(M) prefix | O(K) or O(M) | Batch chunk ≤1000 on upsert; Postgres bind param cap 65535 | error on multi-workspace key batch; O(N) COUNT* fallback |
| `DATA-PG-KV-COUNT-EMBEDDED-CHUNKS-084` | PG | KV | O(log N) single; O(K log N) batch; O(M) prefix | O(K) or O(M) | Batch chunk ≤1000 on upsert; Postgres bind param cap 65535 | error on multi-workspace key batch; O(N) COUNT* fallback |
| `DATA-PG-KV-KEYS-WITH-PREFIX-085` | PG | KV | O(log N) single; O(K log N) batch; O(M) prefix | O(K) or O(M) | Batch chunk ≤1000 on upsert; Postgres bind param cap 65535 | error on multi-workspace key batch; O(N) COUNT* fallback |
| `DATA-PG-KV-KEYS-WITH-PREFIX-LIMITED-086` | PG | KV | O(log N) single; O(K log N) batch; O(M) prefix | O(K) or O(M) | Batch chunk ≤1000 on upsert; Postgres bind param cap 65535 | error on multi-workspace key batch; O(N) COUNT* fallback |
| `DATA-PG-KV-KEYS-WITH-SUFFIX-087` | PG | KV | O(log N) single; O(K log N) batch; O(M) prefix | O(K) or O(M) | Batch chunk ≤1000 on upsert; Postgres bind param cap 65535 | error on multi-workspace key batch; O(N) COUNT* fallback |
| `DATA-PG-KV-KEYS-WITH-SUFFIX-LIMITED-088` | PG | KV | O(log N) single; O(K log N) batch; O(M) prefix | O(K) or O(M) | Batch chunk ≤1000 on upsert; Postgres bind param cap 65535 | error on multi-workspace key batch; O(N) COUNT* fallback |
| `DATA-PG-KV-KEYS-089` | PG | KV | O(log N) single; O(K log N) batch; O(M) prefix | O(K) or O(M) | Batch chunk ≤1000 on upsert; Postgres bind param cap 65535 | error on multi-workspace key batch; O(N) COUNT* fallback |
| `DATA-PG-KV-CLEAR-090` | PG | KV | O(log N) single; O(K log N) batch; O(M) prefix | O(K) or O(M) | Batch chunk ≤1000 on upsert; Postgres bind param cap 65535 | error on multi-workspace key batch; O(N) COUNT* fallback |
| `DATA-PG-KV-TRANSITION-IF-STATUS-091` | PG | KV | O(log N) single; O(K log N) batch; O(M) prefix | O(K) or O(M) | Batch chunk ≤1000 on upsert; Postgres bind param cap 65535 | error on multi-workspace key batch; O(N) COUNT* fallback |
| `DATA-PG-KV-DDL-CREATE-TABLE-092` | PG | KV | O(log N) single; O(K log N) batch; O(M) prefix | O(K) or O(M) | Batch chunk ≤1000 on upsert; Postgres bind param cap 65535 | error on multi-workspace key batch; O(N) COUNT* fallback |
| `DATA-PG-PDF-STORE-093` | PG | PDF | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-PDF-GET-094` | PG | PDF | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-PDF-UPDATE-MARKDOWN-095` | PG | PDF | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-PDF-UPDATE-STATUS-096` | PG | PDF | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-PDF-LINK-TO-DOCUMENT-097` | PG | PDF | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-PDF-LIST-098` | PG | PDF | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-PDF-DELETE-099` | PG | PDF | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-PDF-CLEAR-MARKDOWN-100` | PG | PDF | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-DOCS-ENSURE-RECORD-101` | PG | DOCS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-DOCS-UPDATE-STATS-102` | PG | DOCS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-DOCS-TOUCH-STATUS-103` | PG | DOCS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-DOCS-DELETE-RECORD-104` | PG | DOCS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-PDF-COUNT-105` | PG | PDF | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-DOCS-LIST-SUMMARIES-106` | PG | DOCS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-DOCS-DELETE-WORKSPACE-107` | PG | DOCS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-ORIGINAL-STORE-108` | PG | ORIGINAL | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-MM-ASSET-STORE-109` | PG | MM-ASSET | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-CREATE-110` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-GET-111` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-UPDATE-112` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-DELETE-113` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-LIST-114` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-SHARE-115` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-UNSHARE-116` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-GET-SHARED-117` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-MSG-CREATE-118` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-MSG-UPDATE-119` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-MSG-GET-120` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-MSG-DELETE-121` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-MSG-LIST-122` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-FOLDER-CREATE-123` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-FOLDER-LIST-124` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-FOLDER-UPDATE-125` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-FOLDER-GET-126` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-FOLDER-DELETE-127` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-BULK-DELETE-128` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-BULK-ARCHIVE-129` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONV-BULK-MOVE-130` | PG | CONV | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-TASKS-CREATE-131` | PG | TASKS | O(log N) get; claim O(W + log N) + row lock | O(1) claim; O(page) list | SKIP LOCKED concurrency; Lease TTL reclaim | starvation without fair claim; lease expiry race |
| `DATA-PG-TASKS-GET-132` | PG | TASKS | O(log N) get; claim O(W + log N) + row lock | O(1) claim; O(page) list | SKIP LOCKED concurrency; Lease TTL reclaim | starvation without fair claim; lease expiry race |
| `DATA-PG-TASKS-TOUCH-133` | PG | TASKS | O(log N) get; claim O(W + log N) + row lock | O(1) claim; O(page) list | SKIP LOCKED concurrency; Lease TTL reclaim | starvation without fair claim; lease expiry race |
| `DATA-PG-TASKS-UPDATE-134` | PG | TASKS | O(log N) get; claim O(W + log N) + row lock | O(1) claim; O(page) list | SKIP LOCKED concurrency; Lease TTL reclaim | starvation without fair claim; lease expiry race |
| `DATA-PG-TASKS-DELETE-135` | PG | TASKS | O(log N) get; claim O(W + log N) + row lock | O(1) claim; O(page) list | SKIP LOCKED concurrency; Lease TTL reclaim | starvation without fair claim; lease expiry race |
| `DATA-PG-TASKS-LIST-136` | PG | TASKS | O(log N) get; claim O(W + log N) + row lock | O(1) claim; O(page) list | SKIP LOCKED concurrency; Lease TTL reclaim | starvation without fair claim; lease expiry race |
| `DATA-PG-TASKS-STATS-137` | PG | TASKS | O(log N) get; claim O(W + log N) + row lock | O(1) claim; O(page) list | SKIP LOCKED concurrency; Lease TTL reclaim | starvation without fair claim; lease expiry race |
| `DATA-PG-TASKS-FIND-ACTIVE-PDF-138` | PG | TASKS | O(log N) get; claim O(W + log N) + row lock | O(1) claim; O(page) list | SKIP LOCKED concurrency; Lease TTL reclaim | starvation without fair claim; lease expiry race |
| `DATA-PG-TASKS-FIND-ACTIVE-INGEST-139` | PG | TASKS | O(log N) get; claim O(W + log N) + row lock | O(1) claim; O(page) list | SKIP LOCKED concurrency; Lease TTL reclaim | starvation without fair claim; lease expiry race |
| `DATA-PG-TASKS-CLAIM-NEXT-140` | PG | TASKS | O(log N) get; claim O(W + log N) + row lock | O(1) claim; O(page) list | SKIP LOCKED concurrency; Lease TTL reclaim | starvation without fair claim; lease expiry race |
| `DATA-PG-TASKS-REFRESH-LEASE-141` | PG | TASKS | O(log N) get; claim O(W + log N) + row lock | O(1) claim; O(page) list | SKIP LOCKED concurrency; Lease TTL reclaim | starvation without fair claim; lease expiry race |
| `DATA-PG-TASKS-RELEASE-CLAIM-142` | PG | TASKS | O(log N) get; claim O(W + log N) + row lock | O(1) claim; O(page) list | SKIP LOCKED concurrency; Lease TTL reclaim | starvation without fair claim; lease expiry race |
| `DATA-PG-TASKS-QUEUE-METRICS-143` | PG | TASKS | O(log N) get; claim O(W + log N) + row lock | O(1) claim; O(page) list | SKIP LOCKED concurrency; Lease TTL reclaim | starvation without fair claim; lease expiry race |
| `DATA-PG-TASKS-TOTAL-COUNT-144` | PG | TASKS | O(log N) get; claim O(W + log N) + row lock | O(1) claim; O(page) list | SKIP LOCKED concurrency; Lease TTL reclaim | starvation without fair claim; lease expiry race |
| `DATA-PG-TENANT-CREATE-145` | PG | TENANT | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-TENANT-GET-146` | PG | TENANT | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-TENANT-GET-BY-SLUG-147` | PG | TENANT | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-TENANT-UPDATE-148` | PG | TENANT | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-TENANT-DELETE-149` | PG | TENANT | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-TENANT-LIST-150` | PG | TENANT | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-WORKSPACE-CREATE-151` | PG | WORKSPACE | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-WORKSPACE-GET-152` | PG | WORKSPACE | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-WORKSPACE-GET-BY-SLUG-153` | PG | WORKSPACE | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-WORKSPACE-UPDATE-154` | PG | WORKSPACE | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-WORKSPACE-DELETE-155` | PG | WORKSPACE | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-WORKSPACE-LIST-156` | PG | WORKSPACE | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-AGE-WORKSPACE-GET-STATS-157` | AGE | WORKSPACE | O(K log N) batch ID; O(branch^depth) expand; O(N) full scan FORBIDDEN | O(K) or O(branch^depth) | Native writes preferred (UNIQUE node_id); Cypher MERGE debug-only | cartesian expansion / timeout / OOM on unbounded MATCH |
| `DATA-PG-MEMBERSHIP-ADD-158` | PG | MEMBERSHIP | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-MEMBERSHIP-GET-USER-159` | PG | MEMBERSHIP | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-MEMBERSHIP-GET-TENANT-160` | PG | MEMBERSHIP | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-MEMBERSHIP-UPDATE-ROLE-161` | PG | MEMBERSHIP | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-MEMBERSHIP-REMOVE-162` | PG | MEMBERSHIP | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-MEMBERSHIP-CHECK-TENANT-163` | PG | MEMBERSHIP | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-MEMBERSHIP-CHECK-WORKSPACE-164` | PG | MEMBERSHIP | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-MEMBERSHIP-GET-ROLE-165` | PG | MEMBERSHIP | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-QUOTA-UPDATE-TENANT-166` | PG | QUOTA | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-METRICS-RECORD-SNAPSHOT-167` | PG | METRICS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-METRICS-GET-HISTORY-168` | PG | METRICS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-AUTH-SYNC-USER-169` | PG | AUTH | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-AUTH-ENSURE-DEFAULT-TENANT-WS-170` | PG | AUTH | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-AUTH-SYNC-MEMBERSHIP-171` | PG | AUTH | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-AUTH-VERIFY-MEMBERSHIP-172` | PG | AUTH | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-AUTH-LOAD-USER-173` | PG | AUTH | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-AUTH-FIND-USER-BY-LOGIN-174` | PG | AUTH | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-AUTH-LIST-USERS-175` | PG | AUTH | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-AUTH-DELETE-USER-176` | PG | AUTH | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-SESSION-PERSIST-REFRESH-177` | PG | SESSION | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-SESSION-LOAD-REFRESH-178` | PG | SESSION | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-SESSION-REVOKE-REFRESH-179` | PG | SESSION | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-SESSION-PERSIST-API-KEY-180` | PG | SESSION | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-SESSION-LIST-API-KEYS-181` | PG | SESSION | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-SESSION-FIND-API-KEY-PREFIX-182` | PG | SESSION | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-SESSION-REVOKE-API-KEY-183` | PG | SESSION | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-ENTITY-UPSERT-184` | PG | ENTITY | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-ENTITY-REMOVE-SOURCES-185` | PG | ENTITY | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-LINEAGE-RECORD-ENTITY-LINK-186` | PG | LINEAGE | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-LINEAGE-RECORD-RELATION-LINK-187` | PG | LINEAGE | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-LINEAGE-RECORD-RELATION-LINKS-BATCH-188` | PG | LINEAGE | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-LINEAGE-RECORD-ENTITY-LINKS-BATCH-189` | PG | LINEAGE | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-LINEAGE-APPEND-DESC-HISTORY-190` | PG | LINEAGE | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-LINEAGE-LOAD-DOC-FROM-CHUNKS-191` | PG | LINEAGE | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-FAILED-CHUNKS-INSERT-192` | PG | FAILED-CHUNKS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-FAILED-CHUNKS-LIST-193` | PG | FAILED-CHUNKS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-FAILED-CHUNKS-MARK-STATUS-194` | PG | FAILED-CHUNKS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-RLS-SET-TENANT-CONTEXT-195` | PG | RLS | O(1) | O(1) | SET LOCAL only inside short transactions; Do not leak GUCs on pooled conns | GUC leak → wrong recall/plan for next borrower |
| `DATA-PG-RLS-CLEAR-TENANT-CONTEXT-196` | PG | RLS | O(1) | O(1) | SET LOCAL only inside short transactions; Do not leak GUCs on pooled conns | GUC leak → wrong recall/plan for next borrower |
| `DATA-PG-POOL-ACQUIRE-CONNECT-197` | PG | POOL | O(1) | O(1) | SET LOCAL only inside short transactions; Do not leak GUCs on pooled conns | GUC leak → wrong recall/plan for next borrower |
| `DATA-PG-AUDIT-WRITE-EVENT-198` | PG | AUDIT | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-AUDIT-QUERY-LOGS-199` | PG | AUDIT | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONFIG-LOAD-LLM-DEFAULTS-200` | PG | CONFIG | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONFIG-SAVE-LLM-DEFAULTS-201` | PG | CONFIG | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONFIG-LOAD-PRIORITY-MODE-202` | PG | CONFIG | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-CONFIG-SAVE-PRIORITY-MODE-203` | PG | CONFIG | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-KEYWORDS-CACHE-GET-204` | PG | KEYWORDS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-KEYWORDS-CACHE-SET-205` | PG | KEYWORDS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-KEYWORDS-CACHE-DELETE-206` | PG | KEYWORDS | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-KEYWORDS-CACHE-INIT-207` | PG | KEYWORDS | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-STATS-ENSURE-ROW-COUNT-208` | PG | STATS | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-ID-ALLOCATE-DOCUMENT-209` | PG | ID | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-INSPECT-CHECK-EXTENSIONS-210` | PG | INSPECT | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-INSPECT-CHECK-TABLES-211` | PG | INSPECT | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-INSPECT-CHECK-INVARIANTS-212` | PG | INSPECT | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-INSPECT-APPLY-REPAIR-213` | PG | INSPECT | O(log N) indexed; O(N) scan | O(K) | Use keyset pagination not large OFFSET; statement_timeout recommended | timeout / lock wait / OOM on unbounded SELECT |
| `DATA-PG-SCHEMA-MIGRATE-RUNNER-214` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-INIT-BASE-215` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-TASKS-TABLE-216` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-CONVERSATION-TABLE-217` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-AUDIT-LOG-218` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-RLS-POLICIES-219` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-AGE-GRAPH-220` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-FULLTEXT-SEARCH-221` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-FAILED-CHUNKS-222` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-PDF-DOCUMENTS-223` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-VECTOR-BTREE-INDEXES-224` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-SOURCE-IDS-GIN-225` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-CQRS-ENTITIES-226` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-CHUNK-LINEAGE-227` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-AGE-INDEXES-CONSOLIDATE-228` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-HNSW-OPTIMIZE-229` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-HALFVEC-EMBEDDINGS-230` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-DOCUMENT-ORIGINALS-231` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-MM-ASSETS-232` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-TASK-LEASE-233` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-MERGE-GRAPH-PROPS-234` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
| `DATA-PG-SCHEMA-MIG-EQ-ID-DENORM-235` | PG | SCHEMA | O(N log N) HNSW build; O(N) btree | maintenance_work_mem | Never REINDEX on request path; max_parallel_maintenance_workers for HNSW (CVE floor ≥0.8.2) | lock contention ACCESS EXCLUSIVE; build OOM |
