# Benchmark 001 — `DATA-PGVEC-VECTORS-ANN-QUERY-001`

**Entry:** `VectorStorage::query` @ `edgequake/crates/edgequake-storage/src/adapters/postgres/vector/storage_impl.rs:28`

## Complexity

- Variables: N=rows, K=top_k, D=dim, ef=ef_search
- Time: O(ef * log N) expected ANN; worst O(N) if seq scan
- Space: O(K + ef)
- I/O: ~O(ef) random pages; index residency critical

## EXPLAIN evidence

Representative plan shape (capture with live DB):

```sql
/* DATA-PGVEC-VECTORS-ANN-QUERY-001 */ EXPLAIN (ANALYZE, BUFFERS, VERBOSE, SETTINGS, FORMAT TEXT)
-- bind representative parameters for this operation
```

### Expected access path

- Engine: PGVEC
- Prefer index path documented in indexes.md
- Fail test if plan regresses to Seq Scan on N > threshold (see Phase 3)

## Scaling (relative)

| N | Relative latency class |
|---|---|
| 1e3 | baseline |
| 1e5 | ≤ 5× baseline (log-ish) for indexed ops |
| 1e6 | ≤ 10× baseline for ANN/PK; fail if linear full scan |

Absolute wall-clock lives in non-blocking nightly benchmark job.
